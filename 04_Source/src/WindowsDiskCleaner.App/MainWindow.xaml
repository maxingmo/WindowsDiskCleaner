<Window x:Class="WindowsDiskCleaner.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Scanner P2 - Search and Filter"
        Height="720"
        Width="1180"
        MinHeight="520"
        MinWidth="920">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="磁盘文件扫描"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12" />

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="96" />
                <ColumnDefinition Width="96" />
                <ColumnDefinition Width="96" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Height="32"
                     VerticalContentAlignment="Center"
                     Text="{Binding RootPath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="1"
                    Margin="8,0,0,0"
                    Content="选择目录"
                    Command="{Binding BrowseCommand}" />
            <Button Grid.Column="2"
                    Margin="8,0,0,0"
                    Content="开始扫描"
                    Command="{Binding ScanCommand}" />
            <Button Grid.Column="3"
                    Margin="8,0,0,0"
                    Content="取消"
                    Command="{Binding CancelCommand}" />
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Files}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="文件名" Binding="{Binding Name}" Width="190" />
                <DataGridTextColumn Header="大小" Binding="{Binding SizeDisplay}" Width="100" SortMemberPath="SizeBytes" />
                <DataGridTextColumn Header="扩展名" Binding="{Binding Extension}" Width="90" />
                <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAt}" Width="150" />
                <DataGridTextColumn Header="修改时间" Binding="{Binding ModifiedAt}" Width="150" />
                <DataGridTextColumn Header="完整路径" Binding="{Binding FullPath}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3"
                BorderBrush="#D7DCE2"
                BorderThickness="1"
                Padding="10"
                Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusText}" TextWrapping="Wrap" />
        </Border>
    </Grid>
</Window>
